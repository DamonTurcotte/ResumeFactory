import { variant0, colorScheme0, editSectionOrder0 } from "./0";

const templateVariants = {
  0: variant0,
};

const templateColors = {
  0: colorScheme0,
};

const templateReorders = {
  0: editSectionOrder0,
};

export {
  templateVariants,
  templateColors,
  templateReorders,
};